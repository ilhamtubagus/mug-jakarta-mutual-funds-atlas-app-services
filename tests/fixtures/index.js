const mockInvestmentManagers = require('./mockInvestmentManagers');
const mockProducts = require('./mockProducts');
const mockNavs = require('./mockNavs');
const mockPortfolios = require('./mockPortfolios');

module.exports = {
  mockInvestmentManagers,
  mockProducts,
  mockNavs,
  mockPortfolios,
};
